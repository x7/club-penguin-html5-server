const clientName = '/client'
const serverName = '/server'

export const CLIENT_ROUTES = {
    CLIENT_TEST: `${clientName}/test`
}

export const SERVER_ROUTES = {
    SERVER_TEST: `${serverName}/test`
}